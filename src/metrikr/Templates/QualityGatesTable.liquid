# Module Quality Gates

This page is a collection of quality gate badges of SonarQube.
{% for tag in Tags %}
## {{ tag.Name }} Quality Gates

| Module    | Description  | Quality Gate |
| --------- | ------------ | ------------ |
{% for entry in tag.Entries -%}
| {{ entry.ProjectName }} | {% if entry.ProjectDescription != "" %}{{ entry.ProjectDescription }}{% else %}-{% endif %} | [![Quality Gate Status]({{ entry.SonarQubeDomain }}/api/project_badges/measure?project={{ entry.ProjectId }}&metric=alert_status&token={{ entry.BadgeToken }})]({{ entry.SonarQubeDomain }}/dashboard?id={{ entry.ProjectId }}) |
{% endfor -%}
{% endfor -%}
